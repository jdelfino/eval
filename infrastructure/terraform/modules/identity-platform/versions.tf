# Terraform and Provider Version Constraints
#
# This module requires the Google Cloud provider for Identity Platform resources.
# Note: google-beta is included for potential future features.

terraform {
  required_version = ">= 1.5.0"

  required_providers {
    google = {
      source  = "hashicorp/google"
      version = "~> 5.0"
    }
    google-beta = {
      source  = "hashicorp/google-beta"
      version = "~> 5.0"
    }
  }
}
