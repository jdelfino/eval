# Terraform and Provider Version Constraints
#
# This module requires the Google Cloud provider for Workload Identity Federation.

terraform {
  required_version = ">= 1.5.0"

  required_providers {
    google = {
      source  = "hashicorp/google"
      version = "~> 5.0"
    }
  }
}
