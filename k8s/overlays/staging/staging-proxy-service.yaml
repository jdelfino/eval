apiVersion: v1
kind: Service
metadata:
  name: staging-proxy
  namespace: staging
spec:
  type: NodePort
  selector:
    app: staging-proxy
  ports:
    - port: 80
      targetPort: 8080
      nodePort: 30080
