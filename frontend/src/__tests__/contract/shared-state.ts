/**
 * Shared state across contract test files.
 * The setup test populates these; other tests read them.
 */
export const state = {
  namespaceId: '',
  classId: '',
  sectionId: '',
  sessionId: '',
  adminUserId: '',
  joinCode: '',
};
